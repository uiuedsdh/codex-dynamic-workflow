# Codex Dynamic Workflow

> **AI-generated project:** This project, including its implementation and documentation, was generated by AI. Review and test it for your environment before production use.

> **Unofficial project:** This independent community project is not affiliated with, maintained by, or endorsed by OpenAI.

> **Security warning:** Workflow definitions can execute local commands and launch Codex agents that read or modify files within their configured sandbox. Treat workflow YAML and prompts as executable code, and run only definitions from repositories you trust.

`codex-dynamic-workflow` is a global Codex Skill for defining reusable project-local workflows as version-controlled YAML DAGs. It runs Codex headlessly, coordinates serial and parallel work, and exposes deterministic commands for starting runs, inspecting progress, retrieving results, and continuing the parent task.

## Motivation and inspiration

This project is inspired by the central idea behind dynamic workflows: move orchestration out of the main model's turn and into a deterministic runtime. Large agent tasks become difficult to manage when the main model must repeatedly decide what to run next and receive every intermediate result in its conversation context. As the number of workers and stages grows, orchestration consumes context, intermediate outputs compete for attention, and a long-running process becomes difficult to inspect or resume.

The useful architectural separation is:

- A deterministic runtime owns control flow, concurrency, barriers, loops, retries, and intermediate state.
- Language-model agents are workers invoked only at the nodes that require reasoning or coding.
- The main interactive agent starts the work, leaves the runtime to execute it, and later reads compact progress or terminal results.
- A stable orchestration can be reviewed, versioned with the project, and reused instead of being reconstructed turn by turn.

`codex-dynamic-workflow` applies those ideas to Codex, but it is not a Claude Code port, implementation, or compatibility layer. Claude Code Dynamic Workflows are described as generated JavaScript orchestration using primitives such as pipelines and parallel groups. This project deliberately uses a constrained, declarative YAML DAG and a deterministic Python runner that launches `codex exec` in headless mode. The narrower definition format trades arbitrary program expressiveness for validation, auditability, bounded expansion, persisted progress, and predictable Git integration.

| Concern | This project's approach |
| --- | --- |
| Reusable definition | Project-owned YAML, prompts, and JSON Schemas committed under `.agents/codex-dynamic-workflows/` |
| Runtime | A local Python process; no model is responsible for scheduling the DAG |
| Agent execution | Independent `codex exec` calls with per-node model, sandbox, timeout, and retry settings |
| Pipeline behavior | Dependencies allow each ready item to advance without waiting for unrelated work |
| Parallel barriers | Explicit `barrier` nodes provide `all_success`, `all_terminal`, or `minimum_success` fan-in |
| Dynamic work | Bounded `foreach` expansion and bounded condition-driven loops |
| Intermediate state | Atomic state snapshots and append-only events outside the model context |
| Completion handoff | `$codex-dynamic-workflow result` retrieves outputs; `$codex-dynamic-workflow continue` lets the main Codex agent actively continue a later turn |

The project does not automatically wake a completed Codex conversation. Completion is intentionally pull-based: the user or main agent queries status or results and then continues the task explicitly.

## Problems it is designed to solve

The workflow layer is useful when the process is stable enough to encode and too large, parallel, or repetitive to coordinate reliably in a single agent turn. Typical examples include:

- **Repository-wide audits:** fan out security, correctness, dependency, performance, or API checks by package or file; independently verify findings; then aggregate only validated results.
- **Large migrations:** partition framework upgrades, API deprecations, mechanical refactors, or language migrations into isolated worktree lanes and merge reviewed changes explicitly.
- **Implement-review-fix loops:** run an implementer, one or more structured verifiers, and a bounded fixer loop until a declared condition passes or the iteration budget is exhausted.
- **Batch and long-tail cleanup:** discover a runtime-sized item list, process it with bounded `foreach` workers, retry transient failures, and retain per-item terminal state.
- **Pipeline versus barrier workloads:** let fast items continue immediately through independent stages while using a barrier only when a later stage truly needs the complete set, a minimum quorum, or all terminal outcomes.
- **Repeatable project procedures:** store release checks, compatibility verification, multi-package maintenance, or other fixed engineering playbooks with the repository so later Codex sessions can run the same reviewed process.
- **Long-running headless work:** start a detached workflow, inspect DAG-only progress without loading prompts or model reasoning, and retrieve a compact structured result in a later turn.

It is generally a poor fit for a one-step edit, an exploratory task that needs frequent human decisions, an unbounded autonomous loop, or a high-risk operation whose intermediate actions require manual approval. Those cases should remain in an interactive Codex turn or be split into smaller workflows with explicit checkpoints.

## Features

- Serial and parallel DAG execution with explicit dependency barriers.
- `agent`, `command`, `barrier`, `foreach`, bounded `loop`, and Git `merge` nodes.
- Detached background runs driven by `codex exec`.
- Per-agent model, sandbox, timeout, retry, and worktree-lane configuration.
- Deterministic DAG progress without reading model reasoning or inferring state from file changes.
- Structured results that a main Codex turn can retrieve and continue from.
- Isolated Git worktrees for concurrent writers and explicit patch, branch, or source merge outputs.
- Atomic state snapshots, append-only events, cancellation, and interrupted-run recovery.

## Requirements

- [Codex CLI](https://developers.openai.com/codex/cli/) installed and authenticated.
- [uv](https://docs.astral.sh/uv/) for the self-contained Python runner.
- Git for repository discovery and worktree-based write lanes.

The runner declares and installs its Python dependencies through PEP 723 metadata; no project virtual environment is required.

## Installation

Clone the repository into the global Codex Skills directory:

```bash
git clone https://github.com/uiuedsdh/codex-dynamic-workflow.git \
  ~/.codex/skills/codex-dynamic-workflow
```

Start a new Codex session after installation so the Skill catalog is refreshed. The command surface is:

```text
$codex-dynamic-workflow <action> [arguments]
```

## Project layout

Workflow definitions belong to the project using them; the runner remains in this global Skill.

```text
your-project/
└── .agents/
    └── codex-dynamic-workflows/
        └── verify-project/
            ├── workflow.yaml
            ├── prompts/
            └── schemas/
```

## Minimal workflow

Create `.agents/codex-dynamic-workflows/verify-project/workflow.yaml` in a Git repository:

```yaml
api_version: codex.workflow/v1
name: verify-project
description: Run independent checks and collect their terminal state.

execution:
  max_concurrency: 2
  failure_policy: continue_independent
  timeout_seconds: 1800

result:
  outputs:
    summary: "${{ nodes.summarize.output }}"

nodes:
  - id: lint
    type: command
    command: ["make", "lint"]
    timeout_seconds: 600

  - id: test
    type: command
    command: ["make", "test"]
    timeout_seconds: 1200

  - id: checks-finished
    type: barrier
    depends_on: [lint, test]
    policy:
      mode: all_terminal

  - id: summarize
    type: command
    depends_on: [checks-finished]
    command: ["python3", "-c", "import json; print(json.dumps({'finished': True}))"]
    capture_json: true
```

Validate and inspect the DAG before running it:

```text
$codex-dynamic-workflow validate verify-project
$codex-dynamic-workflow plan verify-project
$codex-dynamic-workflow start verify-project
```

`start` returns a run ID immediately. Use it to inspect the run and retrieve its terminal result:

```text
$codex-dynamic-workflow status <run-id>
$codex-dynamic-workflow result <run-id>
$codex-dynamic-workflow continue <run-id> apply the completed result
```

## Commands

| Command | Purpose |
| --- | --- |
| `start <workflow> [key=value ...]` | Validate and start a detached run. |
| `status [run-id]` | Report deterministic DAG progress. |
| `result [run-id]` | Return terminal structured outputs without continuing work. |
| `continue [run-id] [instruction]` | Retrieve a terminal result and continue the current Codex turn. |
| `runs` | List recent runs for the current repository. |
| `cancel [run-id]` | Request cancellation. |
| `resume [run-id]` | Recover and resume an interrupted run. |
| `list` | List project-local workflow definitions. |
| `validate <workflow>` | Validate YAML, references, schemas, and DAG constraints. |
| `plan <workflow> [key=value ...]` | Print the resolved execution plan without running it. |

When a run ID is omitted, lifecycle commands select the newest run associated with the current Git repository.

## Agent nodes and model selection

Agent nodes invoke `codex exec`. Each node can choose its own model and sandbox:

```yaml
- id: review
  type: agent
  prompt: prompts/review.md
  output_schema: schemas/review.json
  sandbox: read-only
  model: gpt-5.6-terra
  timeout_seconds: 900
  retry:
    max_attempts: 2
    delay_seconds: 5
```

Omitting `model` uses the model configured for the Codex CLI. A `workspace-write` agent must declare a `lane`; successful write nodes receive a checkpoint commit in their isolated worktree.

## Parallelism, barriers, and loops

Nodes become runnable only after their dependencies satisfy the required policy. Multiple dependencies provide an implicit all-success barrier; explicit `barrier` nodes additionally support:

- `all_success`
- `all_terminal`
- `minimum_success`

`foreach` expands a bounded template over structured upstream items. `loop` repeats a predeclared sequential body until a structured condition or exit-code condition succeeds. Both constructs require explicit limits, so a generated run cannot expand indefinitely.

See [references/workflow-format.md](references/workflow-format.md) for the complete YAML contract, templates, merge behavior, and runtime state model.

## Progress and state

Runtime state defaults to:

```text
~/.codex/workflow-runs/<repository-id>/<run-id>/
```

Set `CODEX_WORKFLOW_STATE_DIR` to redirect it. Progress comes exclusively from persisted node states and sealed dynamic groups. While a `foreach` or loop group is still expanding, status reports counts but does not claim a stable completion percentage.

The state directory contains atomic snapshots, append-only events, structured results, worker diagnostics, artifacts, isolated worktrees, and runner metadata. Prompts, model reasoning, secrets, and inferred file activity are not used as progress signals.

## Safety model

- Workflow definitions are executable automation and must come from a trusted repository.
- Command nodes use argument arrays and never invoke a shell implicitly.
- Agent sandboxes are restricted to `read-only` and `workspace-write`.
- Concurrent writers operate in isolated Git worktree lanes.
- Workflow commits use the repository's effective Git `user.name` and `user.email`; writing workflows fail before execution when either identity is unavailable.
- Source updates require explicit runtime authorization and safety checks.
- Workflow definitions, prompts, state, events, and results must not contain secrets.
- Model-generated or arbitrary Python, shell interpolation, and Jinja-based DAG definitions are rejected.

## Development

Validate the Skill metadata:

```bash
python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
```

Run the complete test suite:

```bash
uv run --with 'PyYAML==6.0.3' --with 'jsonschema==4.26.0' \
  python -m unittest discover -s tests -p 'test_*.py'
```

The tests use a fake Codex executable and do not make model API calls.

## License

Released under the [MIT License](LICENSE).
